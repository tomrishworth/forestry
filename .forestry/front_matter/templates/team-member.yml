---
label: Team Member
hide_body: true
fields:
- name: name
  type: text
  config:
    required: false
  label: Name
- name: role
  type: text
  config:
    required: false
  label: Role
- name: image
  type: file
  config:
    maxSize: 250
  label: Image
- name: bio
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Bio
